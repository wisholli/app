import style from './Menu.module.css'

const Menu = (props) => {
    return (
        <div className={style.menu}>
            <h1 className={style.title}>Menu</h1>
        </div>
    );
}

export default Menu